<div>
  <%# TODO: must be changed for avoiding empty and not completed comment when comment adding form is before list of comments %>
  <%= form_for ([ticket, ticket.comments.build]) do |f| %>
    <%= f.hidden_field :owner_id, value: session[:user_id] %>
    <%= f.text_area :body, cols: 120, rows: 5 %>

    <div style="margin-left: 20px;">
        <%= f.submit %>
    </div>
    <% end %>
</div>
