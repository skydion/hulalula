<!-- <h2>Listing tickets</h2> -->

<table border='1'>
  <thead>
  <tr>
    <th width=10%>UUID</th>
    <th width=15%>State</th>
    <th width=5%>Owner</th>
    <th width=10%>Username</th>
    <th width=10%>E-mail</th>
    <th width=15%>Subject</th>
    <th>Problem</th>
    <th colspan="3">Actions</th>
  </tr>
  </thead>

  <tbody>
  <% @tickets.each do |ticket| %>
      <%= render partial: 'tickets/table_row', locals: {ticket: ticket} %>
  <% end %>
  </tbody>
</table>

<%#= link_to 'Home', action: 'index', controller: 'welcome' %>
